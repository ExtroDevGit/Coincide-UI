# Button

Click action control, with optional confirm and horizontal button chaining.

## Entry point
- `Section:Button({ Name, Callback, Confirm?, Tooltip?, Dependency? })`

## Chaining
- `Button:Button({ Name, Callback, Confirm? })`
