# Settings Window

Panel for settings, config handling, and theme/application controls.

## Entry point
- `Library:SettingsWindow({ Title, Width, Height })`

## Methods
- `Section({ ... })`
- `ApplySettings()`
- `SetVisible(boolean)`
- `SyncWithWindow(window)`
- `PositionNextTo(window)`
- `Destroy()`
