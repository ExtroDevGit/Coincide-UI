# Dropdown

Single- or multi-select list control.

## Entry point
- `Section:Dropdown({ Name, Options, Default, Multi?, Tooltip?, Dependency?, Callback? })`

## State
- `Get()`
- `Set(value or table)`
- `SetOptions(newOptions)`
