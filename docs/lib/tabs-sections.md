# Tabs and Sections

Tabs group content at the top level; sections group controls inside a tab.

## API
- `Window:Tab({ Name })`
- `Tab:Section({ Name, Side })`

## Notes
- `Side` is typically `"Left"` or `"Right"`
- Sections are the container for controls like toggles, sliders, and dropdowns
