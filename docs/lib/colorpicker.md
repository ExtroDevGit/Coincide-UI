# Colorpicker

Color and alpha picker for UI or feature styling.

## Entry point
- `Section:Colorpicker({ Name, Default, Alpha?, Callback?, Dependency? })`

## State
- `Get()` returns `(Color3, alpha)`
- `Set(color, alpha?)`
